# E-sports-01 工程协作规范

本文件适用于仓库根目录及其全部子目录，是本项目后续分析、设计、编码、测试、文档和交付的强制检查清单。系统、开发者和用户在当前会话中的更高优先级指令仍优先于本文件。

附件中从“Agent 观测与质量闭环后续开发提示词”开始的内容针对外部仓库 `yiting001/agent`，不属于本项目需求，不得将其中的业务范围、基线提交或 PR 目标套用到本仓库。

## 一、沟通与上下文

1. 每次重要进展和最终汇报必须以“主人”开头，再说明完成了什么，避免丢失上下文。
2. 使用中文沟通、编写业务文档和交付说明；代码命名遵循仓库现有规范。
3. 开始开发前必须复述：
   - 业务目标；
   - 已有能力；
   - 准备复用的模块；
   - 本次实现边界和明确非目标。
4. 遇到需求不合理、现有实现冲突、安全风险、数据风险或无法验证的情况，必须明确说明，不得擅自猜测或用临时方案掩盖。
5. 未经用户明确要求，不得扩大业务范围、修改无关模块或执行与当前目标无关的外部操作。

## 二、项目技术栈与目录

- 包管理：pnpm workspace，Node.js >= 20，pnpm >= 9。
- 服务端：`apps/server`，NestJS、TypeScript、TypeORM、PostgreSQL、Redis。
- 管理端：`apps/web`，Vue 3、Pinia、Vue Router、Vite、Element Plus。
- C 端：`apps/client`，Vue 3、Pinia、Vue Router、Vite。
- 共享契约：`packages/contracts`，集中维护 DTO、枚举、权限码、事件名和前后端共享类型。
- 文档：`docs/`，入口为 `docs/README.md`。
- 测试目标栈：Jest、Vitest 和项目已有 E2E 工具；新增前先确认仓库是否已经配置。

必须优先使用仓库已经采用的依赖、工具、组件和设计方式。添加依赖前，先确认现有代码和平台能力无法满足需求，并说明新增依赖的必要性、维护成本和安全影响。

## 三、开发前必须执行

1. 阅读以下上下文：
   - `docs/README.md`；
   - 与需求直接相关的 `docs/*.md`；
   - 根目录及相关 workspace 的 `package.json`；
   - 相关数据库 migration（若存在）；
   - 相邻模块代码、共享契约和测试。
2. 使用全仓搜索确认是否已经存在相同或相似能力，按以下顺序决策：
   - 直接复用已有实现；
   - 在现有模块和抽象上扩展；
   - 提取确有复用价值的能力；
   - 确认无法复用后才新增模块。
3. 不得直接开始写代码。先给出与变更规模匹配的最小化设计，至少说明：
   - 业务流程；
   - 现有能力与复用点；
   - DDD 分层和模块边界；
   - 数据模型及 migration；
   - API、权限与状态流转；
   - 异常、安全、并发和扩展边界；
   - 测试与验证方案。
4. 设计以当前明确需求为准，不提前实现没有明确价值的未来能力。

## 四、DDD 与模块设计

后端模块遵循仓库现有四层目录：

```text
modules/<module>/
├── domain/          实体、值对象、领域规则、仓储或服务端口
├── application/     用例、编排、应用服务、mapper
├── infrastructure/  TypeORM、缓存、文件、支付、短信及其他外部适配器
└── interfaces/      Controller、DTO、Guard、Gateway 和协议转换
```

强制约束：

1. Domain 不依赖 NestJS、TypeORM、HTTP、数据库或第三方 SDK。
2. Application 只依赖 Domain、共享契约和抽象端口，不直接依赖 Controller、TypeORM Entity 或外部 SDK。
3. Infrastructure 实现数据库、缓存、文件存储及外部系统端口，不向上泄漏实现细节。
4. Interfaces 只负责参数校验、鉴权入口、协议转换和调用用例，不编写业务规则。
5. 模块之间通过公开服务或清晰端口交互，禁止跨层访问其他模块的数据库实体或内部实现。
6. 一个模块只负责一个明确业务领域，一个函数只做一件事。
7. 一个路由对应一个 Controller 文件，不得把多个复杂路由堆在同一 Controller。
8. 单个代码文件不得超过 500 行；接近上限时按职责拆分，不得通过压缩格式或删除必要可读性规避。
9. 依赖注入、策略、适配器、工厂等模式只有在确实降低耦合或提高可扩展性时使用，禁止为了展示模式增加空抽象。
10. 前后端共享类型优先进入 `packages/contracts`，不得复制定义造成漂移。

## 五、代码质量

1. 以可理解性为首要目标，用最少但完整的代码解决真实问题。
2. 禁止炫技、过度设计、重复封装、无意义继承和提前抽象。
3. 项目代码不得散落业务硬编码：
   - 连接信息和部署密钥进入受控环境配置；
   - 业务参数进入配置中心；
   - 状态、权限、事件和类型进入共享枚举或常量；
   - 限制值、策略权重和提示内容集中管理；
   - 不在函数中散落魔法字符串和数字。
4. 遵循现有环境配置边界：业务代码不得直接读取 `process.env`，引导级配置集中在 `apps/server/src/bootstrap/env.config.ts`。
5. 类型必须准确，禁止使用 `any`、模糊类型、动态属性逃逸、无依据的类型断言或绕过类型系统。
6. 所有外部输入必须校验，异常必须转换为清晰的领域错误、应用错误或协议错误，不得静默吞掉关键失败。
7. 安全优先：不得泄露密钥、令牌、用户隐私、跨租户/跨用户数据或内部敏感路径；日志不得记录密钥、完整敏感内容、二进制或 base64 正文。
8. 不得修改测试掩盖实现缺陷，也不得为通过测试添加特殊硬编码。
9. 注释只解释公共接口、复杂算法、关键规则、安全边界和不直观原因；简单代码依靠命名表达，禁止噪声注释。
10. import 位于文件顶部；提交的代码必须可构建并可直接运行。

## 六、数据库与状态变更

1. 数据结构变更必须使用 migration，不得依赖生产环境 `synchronize`。
2. migration 必须包含可执行的 `up` 和合理的 `down`，并考虑已有数据、默认值、唯一索引、外键、锁表和回滚风险。
3. 当前仓库尚未建立 migration 脚本和标准测试命令。首次涉及表结构变更的任务必须先补齐与 TypeORM 相容的 migration 执行机制，不得以 `DB_SYNCHRONIZE=true` 代替正式迁移。
4. 涉及并发、任务、重试或状态机时，必须明确：
   - 状态定义和合法转换；
   - 幂等键；
   - 事务边界；
   - 并发领取或更新策略；
   - 重试、失败恢复和补偿方式。
5. 删除业务数据时，检查关联数据、文件、缓存、索引、审计记录和外部资源是否需要联动清理。

## 七、前端规范

1. Vue 组件负责展示和交互，Pinia 管理跨组件状态，HTTP Gateway/API 模块负责接口调用；不得把请求、状态和复杂业务全部写入页面组件。
2. 优先复用现有组件、样式、store、composable、路由守卫和 API 封装。
3. 用户流程必须覆盖加载、空状态、失败、重试、权限不足、表单校验和危险操作确认。
4. 不得只完成后端而遗漏需求明确要求的前端入口；本阶段有意不做 UI 时，必须在设计和文档中明确说明。
5. 保持管理端与 C 端的职责、认证存储和视觉体系隔离，不得因复用造成状态串用。

## 八、文档要求

每实现一个功能模块，必须同步更新或新增中文文档。按实际功能适用范围至少包含：

1. 功能目标、已实现能力和非目标；
2. 目录结构及各层职责；
3. Mermaid 模块结构图；
4. Mermaid 业务流程图或时序图；
5. 数据模型/ER 图和状态机；
6. API、配置项、权限和安全边界；
7. 异常处理、降级、清理和失败恢复策略；
8. 测试范围与验证结果；
9. 尚未实现项、风险和后续扩展路线。

文档必须与真实代码一致，不得只描述理想设计。修改已有行为时同步更新 `docs/README.md` 索引及相关模块文档。

## 九、测试与验证

1. 为领域规则、应用服务和关键算法增加单元测试。
2. 为数据库、HTTP、权限隔离、失败恢复和核心用户流程增加 E2E。
3. 覆盖正常路径、边界条件、失败路径、越权、并发和幂等场景。
4. 完成后实际运行所有适用的检查，至少包括：

```bash
pnpm lint
pnpm typecheck
pnpm build
pnpm build:server
git diff --check
```

5. 目标检查清单还包括：

```bash
pnpm format:check
pnpm test
```

当前根 `package.json` 尚未定义 `format:check` 和 `test`。在脚本补齐前，交付时必须明确报告“脚本未配置”，不得声称这两项通过；若当前任务引入测试或格式工具，应同步补齐并执行相应脚本。

6. 涉及 migration、应用启动、真实数据库/Redis、HTTP 或关键 UI 流程时，必须验证对应真实入口。不能声称测试通过，除非实际执行并看到成功结果。
7. 测试失败时修复实现根因；不得删除、放宽或改写有效断言来掩盖缺陷。

## 十、Git 与交付

1. 不直接修改或推送 `main`/`master`；功能开发使用独立分支，默认分支名前缀遵循当前工具约定。
2. 只提交当前功能相关文件，不得混入旧报告、临时文件、密钥、备份或无关修改。
3. 禁止使用 `git add .`、跳过 hooks、`--no-verify`、amend 或破坏性 Git 命令。
4. 提交前检查完整 diff、文件行数、格式、测试和构建结果。
5. 功能任务要求交付 PR 时，创建 PR 后检查 CI、冲突、评审意见和部署预览；CI 失败必须修复。若用户要求仅本地修改、远端权限缺失或未授权外部变更，必须明确报告未创建 PR 的原因。
6. 最终汇报必须以“主人”开头，并简要列出：
   - 实现了什么；
   - 复用了什么；
   - 测试和构建结果；
   - 文档位置；
   - PR 链接或未创建原因；
   - 尚未实现、残余风险和阻塞项。

## 十一、交付前强制检查清单

- [ ] 已复述目标、已有能力、复用点和实现边界。
- [ ] 已阅读相关文档、包脚本、相邻代码、migration 和测试。
- [ ] 已全仓搜索相似能力并优先复用。
- [ ] 已给出最小化设计并说明 DDD、数据、接口、权限、状态、安全和测试。
- [ ] 代码无无关改动、业务硬编码、`any`、敏感日志和跨层依赖。
- [ ] 数据结构变更使用可回滚 migration。
- [ ] 前端覆盖适用的加载、空、失败、重试、权限和确认状态。
- [ ] 中文文档与真实实现同步。
- [ ] 已实际运行适用的 lint、typecheck、test、build、migration、启动和主流程验证。
- [ ] 已检查 `git diff --check`、完整 diff 和文件行数。
- [ ] 最终汇报包含实现、复用、验证、文档、PR 和风险。
